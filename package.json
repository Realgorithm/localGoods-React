{
  "name": "localgoods-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Root-level convenience scripts only. Not deployed — Netlify builds from /frontend, Railway builds from /backend.",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "client": "npm run start --prefix frontend",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently \"npm:server\" \"npm:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
